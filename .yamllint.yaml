---
extends: default
ignore: |
  # generated by `vendir sync`
  vendir.lock.yml
  # upstream manifests fetched by `vendir sync`
  vendor/
rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length:
    max: 200
